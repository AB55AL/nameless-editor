pub const iVector = struct {
    x: i32,
    y: i32,
};

pub const uVector = struct {
    x: u32,
    y: u32,
};

pub const vec3 = struct {
    x: f32,
    y: f32,
    z: f32,
};

pub const vec4 = struct {
    x: f32,
    y: f32,
    z: f32,
    w: f32,
};
